---
galaxy_info:
  author: Mariusz Słowiński
  description: Configure system service

  license: GPL-2.0-or-later

  min_ansible_version: 2.1

  platforms:
    - name: GenericLinux
      versions:
        - any
    - name: Ubuntu
      versions:
        - all

  galaxy_tags:
    - service
    - systemctl

dependencies:
  - role: mario_slowinski.ansible_role_software
    software_pkgs: "{{ service_pkgs }}"
